#include <iostream>
#include <string>

int main() {
    std::cout << "Hello, world." << std::endl;
    return 0;
}

// g++ -c hello.cpp
// g++ -o hello hello.o
// creates program with program name, and object.

// to final push to git:
// git add hello.cpp Makefile
// git status
// git commit -m 'Hello works'
// git push
